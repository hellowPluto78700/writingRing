name: Exp10.2.1 CI

on:
  push:
    paths:
      - 'scripts/experiment_10_2_1_l2_membrane_weighted.py'
      - 'scripts/experiment_10_2_1_l2_membrane_weighted/**'
      - 'scripts/bash_script/SNN_Bash/*exp_10_2_1*'
      - 'tests/test_experiment_10_2_1_l2_membrane_weighted_contract.py'
      - 'docs/plans/EXP10_2_1_L2_MEMBRANE_WEIGHTED.md'
      - 'notebooks/experiment_10_2_1_l2_membrane_weighted.ipynb'
      - '.github/workflows/ci-exp10-2-1.yml'
  pull_request:
    paths:
      - 'scripts/experiment_10_2_1_l2_membrane_weighted.py'
      - 'scripts/experiment_10_2_1_l2_membrane_weighted/**'
      - 'scripts/bash_script/SNN_Bash/*exp_10_2_1*'
      - 'tests/test_experiment_10_2_1_l2_membrane_weighted_contract.py'
      - 'docs/plans/EXP10_2_1_L2_MEMBRANE_WEIGHTED.md'
      - 'notebooks/experiment_10_2_1_l2_membrane_weighted.ipynb'
      - '.github/workflows/ci-exp10-2-1.yml'

permissions:
  contents: read

concurrency:
  group: exp10-2-1-ci-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  contract:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'
          cache: pip
          cache-dependency-path: pyproject.toml

      - name: Install CPU test dependencies
        env:
          PIP_DISABLE_PIP_VERSION_CHECK: '1'
        run: |
          python -m pip install torch --index-url https://download.pytorch.org/whl/cpu
          python -m pip install -e '.[test,snn]'

      - name: Validate Exp10.2.1 Slurm scripts
        run: |
          scripts=(
            scripts/bash_script/SNN_Bash/prepare_exp_10_2_1_cpu.bash
            scripts/bash_script/SNN_Bash/run_exp_10_2_1_cpu_array.bash
            scripts/bash_script/SNN_Bash/finalize_exp_10_2_1_cpu.bash
            scripts/bash_script/SNN_Bash/submit_exp_10_2_1_cpu.bash
          )
          for script in "${scripts[@]}"; do
            test -f "$script"
            bash -n "$script"
          done

      - name: Run Exp10.2.1 focused checks
        run: |
          python -m pytest -q \
            tests/test_repository_source_syntax.py \
            tests/test_experiment_10_2_1_l2_membrane_weighted_contract.py
