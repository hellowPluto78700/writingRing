name: Exp11.0 CI

on:
  push:
    paths:
      - 'scripts/experiment_11_0_rsnn_history_internalization.py'
      - 'scripts/experiment_11_0_rsnn_history_internalization/**'
      - 'scripts/bash_script/SNN_Bash/*exp_11_0*'
      - 'tests/test_experiment_11_0_rsnn_history_internalization_contract.py'
      - 'docs/plans/EXP11_0_RSNN_HISTORY_INTERNALIZATION.md'
      - 'notebooks/experiment_11_0_rsnn_history_internalization.ipynb'
      - '.github/workflows/ci-exp11-0.yml'
  pull_request:
    paths:
      - 'scripts/experiment_11_0_rsnn_history_internalization.py'
      - 'scripts/experiment_11_0_rsnn_history_internalization/**'
      - 'scripts/bash_script/SNN_Bash/*exp_11_0*'
      - 'tests/test_experiment_11_0_rsnn_history_internalization_contract.py'
      - 'docs/plans/EXP11_0_RSNN_HISTORY_INTERNALIZATION.md'
      - 'notebooks/experiment_11_0_rsnn_history_internalization.ipynb'
      - '.github/workflows/ci-exp11-0.yml'

permissions:
  contents: read

concurrency:
  group: exp11-0-ci-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  contract:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'
          cache: pip
          cache-dependency-path: pyproject.toml

      - name: Install CPU test dependencies
        env:
          PIP_DISABLE_PIP_VERSION_CHECK: '1'
        run: |
          python -m pip install torch --index-url https://download.pytorch.org/whl/cpu
          python -m pip install -e '.[test,snn]'

      - name: Validate Exp11.0 Slurm scripts
        run: |
          scripts=(
            scripts/bash_script/SNN_Bash/prepare_exp_11_0_cpu.bash
            scripts/bash_script/SNN_Bash/run_exp_11_0_cpu_array.bash
            scripts/bash_script/SNN_Bash/finalize_exp_11_0_cpu.bash
            scripts/bash_script/SNN_Bash/submit_exp_11_0_cpu.bash
          )
          for script in "${scripts[@]}"; do
            test -f "$script"
            bash -n "$script"
          done

      - name: Run Exp11.0 focused checks
        run: |
          python -m pytest -q \
            tests/test_repository_source_syntax.py \
            tests/test_experiment_11_0_rsnn_history_internalization_contract.py
