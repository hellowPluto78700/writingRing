name: CI

on:
  push:
  pull_request:

jobs:
  fast-checks:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install test dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e '.[test,snn]'
          python -m pip install scikit-learn

      - name: Repository syntax checks
        run: |
          python -m pytest -q tests/test_repository_source_syntax.py

      - name: Experiment 0.1 contract checks
        run: |
          python -m pytest -q tests/test_experiment_0_1_contract.py

      - name: withGyro Experiment 0.2 contract checks
        run: |
          python -m pytest -q tests/test_with_gyro_experiment_0_2_contract.py

      - name: Experiment 3.0.2 contract checks
        run: |
          python -m pytest -q tests/test_experiment_3_0_2_contract.py

      - name: Experiment 3.0.3 contract checks
        run: |
          python -m pytest -q tests/test_experiment_3_0_3_contract.py

      - name: Experiment 3.0.4 contract checks
        run: |
          python -m pytest -q tests/test_experiment_3_0_4_contract.py

      - name: Experiment 3.0.5-3.0.7 contract checks
        run: |
          python -m pytest -q tests/test_experiment_3_0_5_to_3_0_7_contract.py

      - name: Experiment 3.1 contract checks
        run: |
          python -m pytest -q tests/test_experiment_3_1_contract.py

      - name: Experiment 3.2 contract checks
        run: |
          python -m pytest -q tests/test_experiment_3_2_contract.py

      - name: Experiment 3.3 contract checks
        run: |
          python -m pytest -q tests/test_experiment_3_3_contract.py

      - name: Experiment 3.4 contract checks
        run: |
          python -m pytest -q tests/test_experiment_3_4_contract.py

      - name: Experiment 3.4.1 contract checks
        run: |
          python -m pytest -q tests/test_experiment_3_4_1_contract.py

      - name: Experiment 4.0 contract checks
        run: |
          python -m pytest -q tests/test_experiment_4_0_contract.py

      - name: Experiment 4.1 and 4.2 contract checks
        run: |
          python -m pytest -q tests/test_experiment_4_1_4_2_contract.py
